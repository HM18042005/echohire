# Rename this file to keystore.properties and fill in your keystore details for release builds.
# Keep the real keystore.properties OUT of version control (add to .gitignore).

storeFile=/absolute/path/to/your/echohire-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=echohire
keyPassword=YOUR_KEY_PASSWORD
